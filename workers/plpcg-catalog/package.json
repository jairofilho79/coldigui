{
  "name": "plpcg-catalog",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "wrangler check",
    "types": "wrangler types",
    "db:migrate:local": "wrangler d1 migrations apply plpcg-catalog --local",
    "db:migrate:remote": "wrangler d1 migrations apply plpcg-catalog --remote",
    "test": "node --experimental-strip-types --test src/auth/username_rules.test.ts src/playlists/publication_rules.test.ts src/coldigom_assets_proxy.test.ts"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250601.0",
    "typescript": "^5.8.3",
    "wrangler": "^4.20.0"
  },
  "dependencies": {
    "jose": "^5.10.0"
  }
}

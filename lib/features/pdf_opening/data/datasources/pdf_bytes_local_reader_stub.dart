import 'dart:typed_data';

/// Stub — substituído por implementação nativa ou web via conditional import.
Future<Uint8List> readLocalPdfBytes(String path) {
  throw UnsupportedError('readLocalPdfBytes não disponível nesta plataforma');
}

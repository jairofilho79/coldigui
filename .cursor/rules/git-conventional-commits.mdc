---
description: Conventional Commit Messages specification for structured commit messages
alwaysApply: false
---

# Conventional Commit Messages

Use the [Conventional Commits](https://www.conventionalcommits.org/) specification when generating commit messages.

## Format

```
<type>[optional scope]: <description>

[optional body]

[optional footer(s)]
```

## Types

- **fix**: patches a bug (PATCH in SemVer)
- **feat**: introduces a new feature (MINOR in SemVer)
- **BREAKING CHANGE**: breaking API change (MAJOR in SemVer) — via footer `BREAKING CHANGE:`, or `!` after type/scope (e.g., `feat!:`)
- Other allowed types: `build:`, `chore:`, `ci:`, `docs:`, `style:`, `refactor:`, `perf:`, `test:`, etc.

## Rules

- Commits MUST be prefixed with a type, optional scope, optional `!`, then colon and space.
- **feat** MUST be used when adding a new feature; **fix** MUST be used for bug fixes.
- Scope is optional, in parentheses: `fix(parser):`
- Description MUST immediately follow the colon and space.
- Body MAY follow after one blank line; footers MAY follow after one blank line.
- Footer tokens use `-` instead of spaces (except `BREAKING CHANGE`).
- Types are case-insensitive except `BREAKING CHANGE` which MUST be uppercase.
- `BREAKING-CHANGE` is synonymous with `BREAKING CHANGE` in footers.

## Examples

```
feat(auth): add biometric login support

fix(router): prevent duplicate navigation on rapid tap

feat(api)!: remove deprecated v1 endpoints

BREAKING CHANGE: environment variables now take precedence over config files
```
